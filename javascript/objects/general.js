// Function to execute a callback with an object after 3 seconds
// function ...

// Function to merge two objects without overwriting existing properties
const mergeObj = (obj1, obj2) => {
    // Combine properties of obj1 and obj2
    // Use spread operator to include properties from both objects
}

// Example usage
// console.log(mergeObj({ a: 2, b: 6 }, { a: 3, b: 9 }))

// Function to rename a specific property in an object
const renameProp = (obj, prop, newProp) => {
    
}

// Example usage
// console.log(renameProp({ a: 2, b: 5 }, "b", "bt"))

// Function to reverse the key-value pairs of an object
const reverseKeyValue = obj => {
    
}

// Example usage
// console.log(reverseKeyValue({ a: 1, b: 6 }))

// Function to convert an object to a query string
const objToQueryStr = obj => {
    // Convert object entries to an array of key-value pairs
    // Encode key and value as URI components
    // Join pairs with '&' to form a query string
}

// Example usage
// console.log(objToQueryStr({ name: 'John Doe', age: 30, city: 'Example City' }))
