const arr1 = [1, 5, 8, 7, 1, 6, 6, 8, 7, 1, 7, 6]

// Find duplicates in an array
function duplicate(arr) {
    // Problem: Find all duplicate elements in an array.
    // Example input: [1, 5, 8, 7, 1, 6, 6, 8, 7, 1, 7, 6]
    // Example output: [1, 6, 7, 8]

    // Logic goes here
}

// Example usage
// console.log(duplicate(arr1)); // Expected output: [1, 6, 7, 8]


// Remove duplicates from an array
function removeDup(arr) {
    // Problem: Remove all duplicate elements from an array, returning only unique elements.
    // Example input: [1, 2, 4, 5, 7, 8, 9, 5, 7, 4, 8, 9]
    // Example output: [1, 2, 4, 5, 7, 8, 9]

    // Logic goes here
}

// Example usage
// console.log(removeDup([1, 2, 4, 5, 7, 8, 9, 5, 7, 4, 8, 9])); // Expected output: [1, 2, 4, 5, 7, 8, 9]


// Another approach to find duplicates
function duplicate(arr) {
    // Problem: Determine if there are any duplicate elements in an array.
    // Example input: [1, 5, 8, 7, 1, 6, 6, 8, 7, 1, 7, 6]
    // Example output: true (indicating duplicates are present)

    // Logic goes here
}

// Example usage
// console.log(duplicate(arr1)); // Expected output: true


// Another approach to remove duplicates
function duplicate(arr) {
    // Problem: Remove all duplicate elements from an array, returning only unique elements.
    // Example input: [1, 5, 8, 7, 1, 6, 6, 8, 7, 1, 7, 6]
    // Example output: [1, 5, 8, 7, 6]

    // Logic goes here
}

// Example usage
// console.log(duplicate(arr1)); // Expected output: [1, 5, 8, 7, 6]
