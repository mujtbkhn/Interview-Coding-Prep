// Problem: Create a button that cycles through an array of names
// Requirements:
// 1. Display a button with a name on it
// 2. When the button is clicked, it should display the next name in the array
// 3. After reaching the last name, it should cycle back to the first name
// 4. Use the provided array of names

import React from 'react'

const PrintNames = () => {
    const array = ["mujtaba", "nabil", "affan", "saif", "khan"]
    
    // Add necessary state variable(s) here

    // Implement the button click handler function

    return (
        <div>
            {/* Render a button that displays the current name and updates on click */}
        </div>
    )
}

export default PrintNames